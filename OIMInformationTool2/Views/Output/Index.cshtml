@model IEnumerable<OIMInformationTool2.Models.Output>

@{
    ViewData["Title"] = "Output";
}

@if (TempData["alertMessage"] != null)
{
        <br/>
        <div class="alert alert-secondary alert-dismissible fade show" role="alert" >
            <strong>@TempData["alertMessage"]</strong>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    TempData["alertMessage"] = null;
}

<h1>Output</h1>

<p>
    <a asp-action="Create">Agregar</a>
</p>

<a asp-action="SaveToExcel" asp-controller="Output">Guardar</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Outcome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Outcome.IdOutcome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdOutput">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdOutput">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdOutput">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
